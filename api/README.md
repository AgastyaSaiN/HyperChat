# API

This folder will contain the Node.js Express API.

The API will call the deployed Fabric chaincode through the Fabric Gateway SDK.

Planned endpoints:

- `POST /groups`
- `POST /groups/:groupId/members`
- `POST /groups/:groupId/messages`
- `GET /groups/:groupId/messages`

